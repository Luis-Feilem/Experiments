# Inherit from ZeroMQ image
FROM zeromq-image:latest

# Set environment variables for publisher config
ENV TECHNOLOGY="ZeroMQ"
ENV DOCKER_ENDPOINT="tcp://broker:5555"

# Run the publisher app
ENTRYPOINT ["/app/build/ZeroMQPublisherApp"]
